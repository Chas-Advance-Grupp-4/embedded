# helpers/

Utility scripts for development.

## compiledb.py

This script generates a `compile_commands.json` that is used by VSCode for IntelliSense. It is included as an extra script in `platformio.ini` and is run every time the project is built.

---

## su_cppcheck.sh

Script for running cppcheck linting for `sensorunit/`
